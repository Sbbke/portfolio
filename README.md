## Build Results
The build results include two folders: client and server.
```bash
|- build
|- |- client
|- |- server
|- |- |- index.js
```
I was surprized to know that you need to run a server program to host routing for frontend. Since the github page only support static website rendering, we need to enable SPA (Single Page App) in order to build the result into a index.html.

## SPA
